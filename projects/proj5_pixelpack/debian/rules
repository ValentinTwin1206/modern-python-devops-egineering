#!/usr/bin/make -f

%:
	dh $@

override_dh_auto_build:
	# The Nuitka binary is built separately and placed in dist/.
	# This package just picks up the prebuilt artifact.

override_dh_auto_install:
	# debhelper will pick up the binary from pixelpack.install.
