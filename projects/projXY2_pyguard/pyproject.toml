[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "pyguard"
version = "0.1.0"
description = "Lightweight security middleware for Python web applications."
readme = "README.md"
requires-python = ">=3.9"
license = "MIT"
authors = [
    { name = "Your Name" }
]

dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["src/pyguard"]
