# Weather Dashboard

`weather-dashboard` is a fictional project used by the `uv_dependency_resolution.ipynb` notebook. It exists to demonstrate dependency management, locking, resolution strategies, dependency groups, platform markers, and reproducible environment synchronization with `uv`.

The project intentionally keeps Python code minimal so the notebook can focus on packaging and dependency behavior.
