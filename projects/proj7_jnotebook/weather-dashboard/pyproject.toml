[project]
name = "weather-dashboard"
version = "0.1.0"
requires-python = ">=3.11"

dependencies = [
    "fastapi>=0.115",
    "httpx>=0.28",
    "pydantic>=2.10",
    "rich>=14.0",
]

[dependency-groups]
dev = [
    "pytest",
    "ruff",
    "mypy",
]
